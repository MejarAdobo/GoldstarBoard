[project]
name = "admin_page"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["django>=6.0.6", "psycopg[binary]>=3.3.4", "python-decouple>=3.8"]
