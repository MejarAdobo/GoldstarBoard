services:
    postgres:
        image: postgres:latest
        environment:
            POSTGRES_USER: ${DB_USER}
            POSTGRES_PASSWORD: ${DB_PASSWORD}
            POSTGRES_DB: ${DB_NAME}
        volumes:
            - ./itas_weather_leaderboard:/var/lib/postgresql/data
        ports:
            - "${DB_PORT}:5432"
