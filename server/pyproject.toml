[project]
name = "server"
version = "0.1.0"
description = "Scrape each weather station for data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "requests>=2.32.5",
    "sqlalchemy>=2.0.48",
]
