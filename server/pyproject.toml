[project]
name = "server"
version = "0.1.0"
description = "Backend for ITAS Weather Leaderboard"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "django>=6.0.3",
    "django-apscheduler>=0.7.0",
    "requests>=2.32.5",
]
